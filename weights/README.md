# Weights Directory

Store local teacher and student checkpoints here when they are not pulled from a shared cache.

Recommended layout:
- `weights/teacher/`
- `weights/student/`
- `weights/adapters/`
